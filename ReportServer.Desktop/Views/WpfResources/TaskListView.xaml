<UserControl x:Class="ReportServer.Desktop.Views.WpfResources.TaskListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.Resources>
        <ResourceDictionary Source="TemplatesDictionary.xaml"/>
    </UserControl.Resources>

    <Grid>
        <DataGrid
                  ColumnWidth="*"
                  ItemsSource="{Binding TaskCompacts}" 
                  SelectedItem="{Binding SelectedTaskCompact}">
            <DataGrid.Columns>
                <DataGridTextColumn Header = "Schedule" Binding="{Binding Schedule}"/>
                <DataGridTextColumn Header = "Report" Binding="{Binding ReportName}"/>
                <DataGridTextColumn Header = "RecepientGroup" Binding="{Binding RecepientGroup}"/>
                <DataGridTextColumn Header = "TryCount" Binding="{Binding TryCount}"/>
                <DataGridTextColumn Header = "QueryTimeOut" Binding="{Binding QueryTimeOut}"/>
                <DataGridTextColumn Header = "ReportType" Binding="{Binding ReportType}"/>
                <DataGridTextColumn Header = "HtmlBody" Binding="{Binding HasHtmlBody}"/>
                <DataGridTextColumn Header = "JsonAttachment" Binding="{Binding HasJsonAttachment}"/>
                <DataGridTextColumn Header = "XlsxAttachment" Binding="{Binding HasXlsxAttachment}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
